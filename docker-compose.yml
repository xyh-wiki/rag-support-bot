services:
  knowledge-base-assistant:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./showcase_docs:/app/docs:ro
    environment:
      DOCS_DIR: /app/docs
